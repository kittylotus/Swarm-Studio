# Swarm Studio v0.62.7

## Fixes

- Repositioned the mobile LoRA organize/delete modals so they center within the viewport instead of hugging the bottom safe area.
- Added modal-specific mobile height constraints and scrolling so the organizer actions stay fully visible on smaller screens.

## Notes

- Keeps the v0.62.6 CivitAI hash fallback, batch delete, and review-before-save generate lock behavior intact.
