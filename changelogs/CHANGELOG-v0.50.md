# Swarm Studio v0.50.0

## Changed
- Compact the generation mini-monitor handle to a single line with the live sampler step beside **Generating**.
- Move the random / last-generated seed toggle into the Seed field itself so the action is visually and semantically attached to the parameter it changes.
- Remove the duplicate seed toggle from the Create header/mobile prompt toolbar.

## Validation
- `tsc --noEmit` passes.
