# Swarm Studio v0.49.0

## Fixed
- Restored usable scrolling in the Library output inspector. The inspector body is now the scroll container, so wheel/touch scrolling works from either column.
- Top-aligned the inspected image instead of vertically centering it low in the modal.

## Changed
- Desktop inspector images stay sticky while metadata scrolls past them.
- The inspector header remains fixed above the scrolling body.
- Mobile keeps a single natural scrolling column with the image returning to normal flow.
