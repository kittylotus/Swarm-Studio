# Swarm Studio v0.60.0

## Fixed

- made the Library Load 240 more control sticky near the bottom of the scroll viewport so it cannot disappear beneath the app chrome
- added an explicit trailing Library runway after the load-more footer rather than relying only on container padding
- mobile load-more placement now clears the bottom tab bar and device safe-area inset
- selection mode reserves additional runway for the fixed batch-action rail
