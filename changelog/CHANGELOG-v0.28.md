# v0.28 — Mobile CivitAI sort-sheet hotfix

- Fixes the mobile CivitAI Sort sheet closing whenever a native select control is tapped.
- Backdrop dismissal now fires only when the backdrop itself is tapped.
- Cancel, close, and Save continue to dismiss the sheet normally.
- Sort and Time period selects can now be opened and changed without the sheet self-destructing.
