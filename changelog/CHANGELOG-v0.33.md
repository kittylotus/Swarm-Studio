# Swarm Studio v0.33

- Preserve Models/LoRA browser scroll position across toolbar actions, organizer rerenders, modal opens/closes, and LoRA stack delete/reorder actions on both desktop and mobile.
- Compact the mobile LoRA toolbar so Show non-matching shares a row with folder/select/view controls instead of consuming a full row.
- Add Appearance → LoRA browser → Grid card size with Comfortable and Compact modes. Compact restores a three-column mobile grid and fits more cards per row on desktop.
- Keep the mobile Swarm folder navigator bounded to the LoRA browser section beneath its header/toolbar rather than spanning upward over Models.
