# Swarm Studio v0.27

## Mobile CivitAI toolbar diet

- Replaces the stacked mobile Sort and Period controls with one compact **Sort** action.
- Sort opens a small bottom sheet containing Sort + Time Period and applies both together with **Save**.
- Places **Compatible** and **+18** toggles on one row.
- Turns URL download into a compact labeled button instead of a full-width row.
- Desktop CivitAI controls are unchanged.
