# Swarm Studio v0.46.0

## Fixed
- Add a dedicated Library bottom scroll runway so the final **Load more** control can always scroll clear of the viewport edge.
- Account separately for mobile safe-area insets and the fixed mobile navigation bar.
- Reserve additional trailing space while Library batch selection is active so the fixed action rail cannot cover the final rows or pagination controls.

## Notes
- Full replacement build; only this release changelog is included.
