# Swarm Studio v0.12

UI correction pass.

- Advanced-group descriptions moved into compact clue popovers instead of occupying the rail.
- Preset rows reduced to a title, one apply icon, and a stable overflow menu.
- Corrected the malformed v0.11 CSS append that could let desktop and mobile navigation render together.
- Explicit mobile/desktop navigation ownership at the 760px breakpoint.
- Rebuilt horizontal mobile context swiping so both directions are captured while vertical scrolling remains native.
- Added exit/enter slide transitions for mobile context panels, with reduced-motion support.
