# v0.13 — Tune swipe hotfix

- Fixes the one remaining mobile Create gesture edge: swiping away from the terminal Tune pane now steps back to Output instead of wrapping to Generation or being swallowed by Tune controls.
- Leaves the already-working Generation/Output, Visuals, and Settings swipe behavior untouched.
