# v0.63.21 mini patch — mobile LoRA trigger rail

- Moved the mobile Trigger checkbox into the same right-side control rail as enable/disable and remove.
- Kept the Trigger label directly beneath its checkbox so the three controls read as one compact action group.
- Preserved the metadata + weight two-row content area and the existing narrow-phone density fallback.
- Updated the composer UI contract to guard the mobile control-rail layout.
